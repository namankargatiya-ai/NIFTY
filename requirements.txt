yfinance>=0.2.40
pandas>=2.0.0
numpy>=1.24.0
requests>=2.31.0
nsepython>=2.86
streamlit>=1.35.0

# Upstox integration uses only `requests` above (no official SDK
# required) so no extra dependency is needed for --source upstox.
# If you prefer the official SDK instead of the lightweight wrapper
# in nifty_analyzer/upstox_client.py, you can optionally add:
# upstox-python-sdk>=2.28.0
